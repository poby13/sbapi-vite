<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="kr.co.cofile.sbapivite.mapper.MemberMapper">
    <resultMap id="MemberWithRoles" type="kr.co.cofile.sbapivite.dto.Member">
        <id property="id" column="id"/>
        <result property="email" column="email"/>
        <result property="pw" column="pw"/>
        <result property="nickname" column="nickname"/>
        <result property="social" column="social"/>
        <result property="enabled" column="enabled"/>
        <result property="createdAt" column="created_at"/>
        <result property="updatedAt" column="updated_at"/>
        <collection property="roles" ofType="kr.co.cofile.sbapivite.dto.Role">
            <id property="id" column="role_id"/>
            <result property="name" column="role_name"/>
        </collection>
    </resultMap>
    <select id="getMemberWithRolesById" resultMap="MemberWithRoles">
        SELECT m.id, m.email,
               m.pw,
               m.nickname,
               m.social,
               m.enabled,
               m.created_at,
            m.updated_at,
               r.id as role_id,
               r.role_name
        FROM tbl_members m
                 LEFT JOIN member_roles mr ON m.id = mr.member_id
                 LEFT JOIN tbl_roles r ON mr.role_id = r.id
        WHERE m.email = #{email}
    </select>
</mapper>